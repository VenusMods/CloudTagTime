-When installing for the first time, create an initial prompt so that users aren't "confused" about whether the app is installed or not
-Beeminder integration
-Instead of running subprocesses, import each py file like how the windows exe does it.
-Add replace tag option in log editor
